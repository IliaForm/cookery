<%= form_for(@dish) do |f| %>
  <% if @dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dish.errors.count, "error") %> prohibited this dish from being saved:</h2>

      <ul>
      <% @dish.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :recipe %><br />
    <%= f.text_area :recipe %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br />
  <%=  select("dish", "category_id", Category.all.collect {|c| [ c.name, c.id ] }, {:disabled => 'restricted'}, {:include_blank => false})%>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
